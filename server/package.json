{
  "name": "otoscrapo-server",
  "version": "1.0.0",
  "author": "Dyqmin",
  "description": "",
  "main": "./lib/index.js",
  "scripts": {
    "build": "rimraf lib && tsc",
    "dev": "npm run build && concurrently \"tsc --watch\" \"micro-dev\"",
    "dev-start": "npm run build && micro-dev",
    "start": "npm run build && micro"
  },
  "keywords": [],
  "license": "ISC",
  "dependencies": {
    "micro": "^9.3.4"
  },
  "devDependencies": {
    "@types/micro": "^7.3.3",
    "@types/node": "^14.6.4",
    "concurrently": "^5.3.0",
    "micro-dev": "^3.0.0",
    "rimraf": "^3.0.2",
    "tslint": "^6.1.3",
    "typescript": "^4.0.2"
  }
}
